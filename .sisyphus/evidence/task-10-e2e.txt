Task 10: Monica Import E2E Test
================================

Files Created:
- web/e2e/fixtures/monica_export.json — Simplified Monica export fixture (2 contacts, 2 notes, 1 relationship)
- web/e2e/monica-import.spec.ts — 2 E2E tests for Monica import feature

Test Results:
- 2 passed (18.1s)
  1. should import Monica JSON and show results — uploads fixture, verifies success alert + counts, navigates to contacts list
  2. should show error for invalid JSON file — uploads invalid JSON, verifies error alert

Key Decisions:
- Used fileURLToPath(import.meta.url) instead of __dirname (ESM environment)
- Fixture is minimal: no base64 photos/documents to keep E2E fixture small
- Used .ant-alert-success/.ant-alert-error CSS selectors for reliable alert detection
- Followed existing vault.spec.ts pattern for registerAndCreateVault helper
