# === Essential Configuration ===
# These settings MUST be configured via environment variables.
# All other settings (SMTP, OAuth, Telegram, WebAuthn, Geocoding, etc.)
# can be managed from the Admin Settings page in the web UI.

# --- Debug Mode ---
# Enables: Echo request logging, GORM SQL logging, Swagger UI (/swagger/)
DEBUG=true

# --- Server ---
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# --- Database ---
DB_DSN=bonds.db
DB_DRIVER=sqlite
# For PostgreSQL:
# DB_DRIVER=postgres
# DB_DSN=host=localhost port=5432 user=bonds password=bonds dbname=bonds sslmode=disable

# --- Security ---
# ⚠️ REQUIRED in production. Signing key for JWT auth tokens.
JWT_SECRET=change-me-in-production

# --- Application ---
APP_ENV=development

# --- File Storage ---
# Directory for uploaded files (photos, documents)
# STORAGE_UPLOAD_DIR=uploads

# --- Full-text Search (Bleve) ---
# BLEVE_INDEX_PATH=data/bonds.bleve

# --- Backup ---
# Directory for database backups
# BACKUP_DIR=data/backups

# === Settings managed via Admin UI ===
# The following settings are stored in the database and can be configured
# from the Admin > System Settings page after first login:
#
# - Application: APP_NAME, APP_URL, Announcement banner
# - Authentication: Password auth toggle, Registration toggle
# - JWT: Token expiry, Refresh window
# - SMTP: Host, Port, Username, Password, Sender
# - OAuth: GitHub, Google, OIDC/SSO credentials
# - WebAuthn: Relying Party ID, Display Name, Origins
# - Telegram: Bot token
# - Geocoding: Provider, API key
# - Storage: Max upload size
# - Backup: Cron schedule, Retention days
#
# On first startup, these are seeded from environment variables if set.
# After that, changes should be made through the Admin UI.

